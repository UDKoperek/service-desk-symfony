{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Kategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID kategorii</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">Powróć</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}">Zmień</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
